# First line is the header fields for columns and is mandatory.
# Regular expressions are supported in all fields except the first.
# Last field supports variable length command completion.
# The recommended way to use regular expressions is to surround them with parentheses, for example: abc(x(y)?).
#
Template, Hostname, Vendor, Command
#cisco_bgp_summary_template, .*, Cisco, show ip bgp summary
#cisco_version_template, .*, Cisco, show version
#f10_ip_bgp_summary_template, .*, Force10, show ip bgp summary
#f10_version_template, .*, Force10, show version
#juniper_bgp_summary_template, .*,  Juniper, show bgp summary
#juniper_version_template, .*, Juniper, show version
#unix_ifcfg_template, hostname(abc)?.*, *, ifconfig
